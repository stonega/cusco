#!/bin/sh
script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
prefix=$(dirname "$script_dir")
pkgdatadir=${CUSCO_PKGDATADIR:-"$prefix/share/cusco"}

export GJS_PATH="$pkgdatadir${GJS_PATH:+:$GJS_PATH}"
exec gjs -m "$pkgdatadir/main.js" "$@"
