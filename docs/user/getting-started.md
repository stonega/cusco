# Getting Started

Cusco is currently a development scaffold.

Run it from the project root:

```sh
gjs -m src/main.js
```

The first window has a conversation sidebar, starter transcript, and composer. Sending a message appends it locally and shows a placeholder assistant response until provider integration is added.
