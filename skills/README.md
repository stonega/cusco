# Cusco skills

Place repository-owned skills in `skills/<skill-name>/SKILL.md`. Cusco discovers
this directory independently from global skills in `~/.agents/skills` and from
skills bundled inside installed plugins.

Repository skills appear in Workspace preferences and remain user-controlled.
Set `CUSCO_REPOSITORY_ROOT` when a packaged development build should use a
specific writable Cusco checkout.
