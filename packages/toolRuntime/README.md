# Tool runtime

Tool registration and execution, permission decisions, bounded local file and
shell operations, calculation, structured-data handling, and web search.

Feature adapters such as ask-user and artifacts remain in `src/tools/`.
