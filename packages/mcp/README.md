# MCP

Self-contained Model Context Protocol support for GJS, including stdio and
Streamable HTTP transports, OAuth authorization, configuration normalization,
resource and prompt access, and tool registration.

GTK settings and application presentation stay in `src/settings/`.
