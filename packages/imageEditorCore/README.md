# Image editor core

The image annotation document model, geometry and history operations, Cairo
rendering, image transforms, and PNG export. The libadwaita editor window and
bundled-icon integration remain in `src/imageEditor/`.
