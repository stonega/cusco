# Computer use

GNOME computer-use protocol validation, accessibility snapshots, screenshot
analysis, and the GNOME Shell D-Bus service client.

The AI tool-description adapter and GTK settings remain in `src/computerUse/`
and `src/settings/`.
