# Hooks

Hook discovery, validation, trust state, auditing, process execution, protocol
reduction, and dispatch management. The package has no GTK dependency; Cusco's
settings UI remains in `src/settings/hooksSettings.js`.
